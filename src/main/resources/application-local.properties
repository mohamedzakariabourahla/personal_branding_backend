spring.config.activate.on-profile=local

# Datasource configuration for local development
spring.datasource.url=jdbc:postgresql://localhost:5432/personal-branding-saas
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.jpa.show-sql=true

# Redis defaults when running docker-compose locally
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.ssl.enabled=false

# Local JWT defaults (can be overridden via environment variables)
security.jwt.secret=${SECURITY_JWT_SECRET:local-development-secret}
security.jwt.issuer=${SECURITY_JWT_ISSUER:personal-branding-saas-local}

# UI clients frequently used in development
app.cors.allowed-origins=http://localhost:3000,http://192.168.100.3:3000
app.security.csp=default-src 'self'; img-src 'self' data:; font-src 'self'; script-src 'self'; style-src 'self'; connect-src 'self' http://localhost:3000 http://192.168.100.3:3000 http://192.168.100.3:8080

# Mailtrap (or local SMTP); values pulled from environment variables when set
spring.mail.host=${MAIL_HOST:smtp.mailtrap.io}
spring.mail.port=${MAIL_PORT:2525}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
app.mail.from=${MAIL_FROM:no-reply@local.test}
app.mail.reset-base-url=${APP_MAIL_RESET_BASE_URL:http://localhost:3000/reset-password?token=}
app.mail.verify-base-url=${APP_MAIL_VERIFY_BASE_URL:http://localhost:3000/verify-email?token=}
